name: Test Github Actions

on: push

jobs:
    npm-build:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v4
        - run: pwd
        - run: node --version
        - run: npm i pnpm -g
        - run: pnpm --version
        - run: pnpm i
        - run: pnpm run build
